<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.CommentMapper">

    <select id="getCommentsByReviewIdx" resultType="com.example.demo.model.Comment"> SELECT * FROM
        COMMENTS WHERE board_seq = #{ board_seq}
    </select>

    <!-- CommentMapper.xml에 insert 쿼리 추가 -->
    <insert id="addComment">
        INSERT INTO COMMENTS (comment_seq,board_seq, mem_id, comment, comment_at)
        VALUES (#{comment_seq}, #{board_seq}, #{mem_id}, #{comment}, NOW())
    </insert>

</mapper>